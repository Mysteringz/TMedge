# TMedge

Edge processing for TMnode thermal occupancy sensors.

Work in progress. The full README arrives with the dashboard; see
`src/edge/` for the pipeline and `npm run crosscheck` for the
firmware/edge wire-format check.
